enum RequestStatus { initial, loading, success, failure }

enum OtpStates { initial, loading, success, failure }

enum UploadStatus {
  initial,
  pickingImages,
  imagesSelected,
  uploading,
  uploadSuccess,
  uploadFailure,
}

enum UploadItemStatus {
  initial,
  waiting,
  uploading,
  success,
  failure,
}
